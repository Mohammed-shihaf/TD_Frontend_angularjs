PROJECT TYPE: Design-System Cross-Framework Test Harness (standalone frontend module)
(extracted from the connected TD_Designsystemharness_BE_nodejs_FE_reactjs_FE_angularjs- repo)

BUILD COMMANDS
==============
npm install
npx ng build     # real Angular build, verified passing (Angular 16)
npx ng test --watch=false --browsers=ChromeHeadless   # real karma test suite, verified passing

ARCHITECTURE
============
Fetches GET /api/design-tokens from TD_Backend_nodejs's
designsystemharness branches - the same tokens React fetches
(TD_Frontend_reactjs, same branch name) - and renders using them.
Both renders are parity-tested against each other.

  src/app/app.component.ts     renders using fetched tokens
  quality/                     21 real TypeScript code-quality tool fixtures

Branches: designsystemharness_angularjs<16|17|18|19>
