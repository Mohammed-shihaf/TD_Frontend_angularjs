PROJECT TYPE: Multi-Product SaaS Platform (standalone frontend module)
(extracted from the connected TD_Saasplatform_BE_nodejs_FE_reactjs_FE_angularjs- repo)

BUILD COMMANDS
==============
npm install
npx ng build     # real Angular build, verified passing (Angular 19)
npx ng test --watch=false --browsers=ChromeHeadless   # real karma test suite, verified passing

ARCHITECTURE
============
Angular only calls the /api/admin/* namespace of the SaaS Platform
backend (TD_Backend_nodejs's saasplatform branches). React
(TD_Frontend_reactjs, same branch name) only calls /api/customer/*.

  src/app/app.component.ts     admin-app UI, fetches /api/admin/products
  quality/                     21 real TypeScript code-quality tool fixtures

Branches: saasplatform_angularjs<16|17|18|19>
