PROJECT TYPE: Framework Migration Bridge (standalone frontend module)
(extracted from the connected TD_Migrationbridge_BE_nodejs_FE_reactjs_FE_angularjs repo)

BUILD COMMANDS
==============
npm install
npx ng build     # real Angular build, verified passing (Angular 19)
npx ng test --watch=false --browsers=ChromeHeadless   # real karma test suite, verified passing

ARCHITECTURE
============
This is the LEGACY app in the Migration Bridge: served at /legacy by
TD_Backend_nodejs's migrationbridge branches' route-split backend,
alongside the new React app at /app (TD_Frontend_reactjs, same
branch name).

  src/app/app.component.ts     legacy-app UI, fetches /api/widgets
  quality/                     21 real TypeScript code-quality tool fixtures

Branches: migrationbridge_angularjs<16|17|18|19>
