PROJECT TYPE: White-Label / Multi-Tenant Storefront (standalone frontend module)
(extracted from the connected TD_Whitelabelstorefront_BE_nodejs_FE_reactjs_FE_angularjs- repo)

BUILD COMMANDS
==============
npm install
npx ng build     # real Angular build, verified passing (Angular 22)
npx ng test --watch=false --browsers=ChromeHeadless   # real karma test suite, verified passing

ARCHITECTURE
============
Angular serves ENTERPRISE tenants: fetches GET /api/tenant-config from
TD_Backend_nodejs's whitelabelstorefront branches with x-tenant-id
set to an enterprise tenant. Self-serve tenants get the React
storefront instead (TD_Frontend_reactjs, same branch name).

  src/app/app.component.ts     enterprise storefront UI
  quality/                     21 real TypeScript code-quality tool fixtures

Branches: whitelabelstorefront_angularjs<16|17|18|19>
